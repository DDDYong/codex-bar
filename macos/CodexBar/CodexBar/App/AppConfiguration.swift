import SwiftUI

enum AppConfiguration {
    static let dashboardWindowID = "dashboard"
    static let minimumWindowSize = CGSize(width: 980, height: 640)
    static let defaultWindowSize = CGSize(width: 1083, height: 744)
}
